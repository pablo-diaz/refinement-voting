const Card = ({ label }) => {
    return (
        <>
        <table>
            <tr>
                <td>{label}</td>
            </tr>
        </table>
        </>
    );
}

export default Card;